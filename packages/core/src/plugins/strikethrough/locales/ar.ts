import type { StrikethroughLocale } from '../StrikethroughLocale.js';

const locale: StrikethroughLocale = {
	label: 'يتوسطه خط',
	tooltip: (shortcut: string) => `يتوسطه خط (${shortcut})`,
};

export default locale;
