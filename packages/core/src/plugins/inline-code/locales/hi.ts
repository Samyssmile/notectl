import type { InlineCodeLocale } from '../InlineCodeLocale.js';

const locale: InlineCodeLocale = {
	label: 'इनलाइन कोड',
	tooltip: (shortcut: string) => `इनलाइन कोड (${shortcut})`,
};

export default locale;
